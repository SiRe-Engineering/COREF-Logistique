# Suppressions techniques

Endpoints sans corps JSON :

```text
DELETE /api/stocks/{stock_id}
DELETE /api/preparations/{preparation_id}
DELETE /api/affaires/{affaire_id}
```

Accès réservé au rôle `ADMINISTRATEUR_TECHNIQUE`.
